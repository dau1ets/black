# BLACK — Night Club

React + Vite + Tailwind CSS + Lucide React.

## Запуск

```bash
npm install
npm run dev
```

## GitHub Pages

В репозитории включи **Settings → Pages → Source: GitHub Actions**.
После push в `main` workflow автоматически соберёт и опубликует сайт.
